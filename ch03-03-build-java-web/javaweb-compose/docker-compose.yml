version: '3'
services:
  web:
    image: licerlee/javaweb:v2
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '0.1'
          memory: 50M
      restart_policy:
        condition: on-failure
    ports:
    - 8000:8080
    networks:
    - webnet
networks:
  webnet: 
  